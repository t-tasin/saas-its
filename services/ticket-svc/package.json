{
  "name": "@saas-its/ticket-svc",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "prisma:gen": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  }
}
