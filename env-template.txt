# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/app?schema=identity"
SHADOW_DATABASE_URL="postgresql://user:password@localhost:5432/app?schema=identity_shadow"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="24h"

# Server Configuration
NODE_ENV="development"
PORT=3000

# Email Configuration (for OTP)
# For development, leave these empty to use Ethereal (fake SMTP)
# For production, use real SMTP credentials
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="SaaS ITS <noreply@saas-its.com>"

# Frontend URL (for CORS)
FRONTEND_URL="http://localhost:3004"

