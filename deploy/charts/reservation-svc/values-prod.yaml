# Production values for Reservation Service

image:
  repository: gcr.io/YOUR_PROJECT_ID/reservation-svc  # UPDATE THIS
  tag: v1.0.0
  pullPolicy: IfNotPresent

replicaCount: 2  # High availability

service:
  type: ClusterIP
  port: 5003

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

secrets:
  existingSecret: reservation-svc-secrets

env:
  NODE_ENV: "production"
  PORT: "5003"
  LOG_LEVEL: "info"

