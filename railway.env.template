# Railway Environment Variables Template
# Copy this file to .env for local development
# Set these variables in Railway dashboard for each service
#
# ⚠️ NEVER commit real secrets to Git! This is a TEMPLATE with placeholder values.

# ===========================================
# IDENTITY SERVICE
# ===========================================
# Set these in Railway → identity-svc → Variables

# Database (Update with your Neon/Railway Postgres URL)
DATABASE_URL=postgresql://user:password@host/dbname?schema=identity&sslmode=require

# JWT Secrets (CRITICAL: Generate unique secrets and use same across all services)
# Generate with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_here_generate_with_openssl_rand_hex_32
JWT_REFRESH_SECRET=your_refresh_secret_here_generate_with_openssl_rand_hex_32

# SendGrid Email Configuration
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=your_sendgrid_api_key_here

# Application Config
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# ===========================================
# TICKET SERVICE
# ===========================================
# Set these in Railway → ticket-svc → Variables

# Database (Update schema to 'ticket')
DATABASE_URL=postgresql://user:password@host/dbname?schema=ticket&sslmode=require

# Identity Service URL (UPDATE with your actual Railway URL after deploying identity-svc)
IDENTITY_SVC_URL=https://identity-svc-production.railway.app

# JWT Secret (MUST be same as Identity Service)
JWT_SECRET=your_jwt_secret_here_same_as_identity_svc

# AWS S3 Configuration (Optional - for ticket attachments)
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name

# Application Config
NODE_ENV=production
PORT=3002
LOG_LEVEL=info

# ===========================================
# ASSET SERVICE
# ===========================================
# Set these in Railway → asset-svc → Variables

# Database (Update schema to 'asset')
DATABASE_URL=postgresql://user:password@host/dbname?schema=asset&sslmode=require

# Identity Service URL (UPDATE with your actual Railway URL)
IDENTITY_SVC_URL=https://identity-svc-production.railway.app

# JWT Secret (MUST be same as Identity Service)
JWT_SECRET=your_jwt_secret_here_same_as_identity_svc

# Application Config
NODE_ENV=production
PORT=3003
LOG_LEVEL=info

# ===========================================
# RESERVATION SERVICE
# ===========================================
# Set these in Railway → reservation-svc → Variables

# Database (Update schema to 'reservation')
DATABASE_URL=postgresql://user:password@host/dbname?schema=reservation&sslmode=require

# Identity Service URL (UPDATE with your actual Railway URL)
IDENTITY_SVC_URL=https://identity-svc-production.railway.app

# JWT Secret (MUST be same as Identity Service)
JWT_SECRET=your_jwt_secret_here_same_as_identity_svc

# Application Config
NODE_ENV=production
PORT=5003
LOG_LEVEL=info

# ===========================================
# APPOINTMENTS SERVICE (NEW)
# ===========================================
# Set these in Railway → appointments-svc → Variables

# Database (Update schema to 'appointment')
DATABASE_URL=postgresql://user:password@host/dbname?schema=appointment&sslmode=require

# Google OAuth Credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=https://appointments-svc-production.up.railway.app/auth/google/callback

# Appointment Settings
DEFAULT_TZ=America/New_York
BUSINESS_START=09:00
BUSINESS_END=17:00
APPT_DURATION=30
APPT_LOCATION=IT Desk
HARDWARE_TECH_ID=tech_hardware

# Application Config
NODE_ENV=production
PORT=3400
LOG_LEVEL=info

# ===========================================
# NL-GATEWAY SERVICE
# ===========================================
# Set these in Railway → nl-gateway → Variables

# LLM Configuration
LLM_PROVIDER=groq
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instruct

# Service URLs
TICKET_BASE=https://ticket-svc-production.railway.app/v1
APPT_BASE=https://appointments-svc-production.up.railway.app

# Appointment Integration
APPT_DURATION=30
APPT_TZ=America/New_York
APPT_LOCATION=IT Desk
HARDWARE_TECH_ID=tech_hardware

# Application Config
NODE_ENV=production
PORT=3100
LOG_LEVEL=info

# ===========================================
# IMPORTANT NOTES
# ===========================================
#
# 1. DO NOT commit real secrets to Git!
#    This template uses placeholder values only.
#
# 2. Generate secure JWT secrets:
#    openssl rand -hex 32
#
# 3. JWT_SECRET must be IDENTICAL across all services
#
# 4. Update IDENTITY_SVC_URL after deploying identity service:
#    - Deploy identity-svc first
#    - Get the Railway URL
#    - Update in all other services
#
# 5. For Railway deployment:
#    - Each service gets its own set of environment variables
#    - Copy the relevant section to Railway dashboard
#    - Use "Raw Editor" in Railway Variables tab
#
# 6. Generate new secrets for production!
#    Never use example/default values in production.
#

